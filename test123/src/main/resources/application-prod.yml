spring:
  application:
    name: team-project
  datasource:
    url: jdbc:postgresql://prod_db:5432/prod_main_db
    username: produser
    password: prodpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8585
  error:
    include-stacktrace: always
    include-message: always
    include-binding-errors: always

springdoc:
  swagger-ui:
    path: /api.name/docs/swagger
    enabled: true
    try-it-out-enabled: true
  api-docs:
    path: /api-docs
    enabled: true